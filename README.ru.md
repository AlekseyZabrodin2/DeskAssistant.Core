# DeskAssistant.Core

**Ядро системы управления задачами и календарем**

## 📋 Описание

DeskAssistant.Core - это основная библиотека системы управления задачами, предоставляющая базовые модели, сервисы и контракты для работы с задачами календаря, их приоритезацией и уведомлениями через Telegram.

## 🏗️ Архитектура проекта

### Структура решения

```
DeskAssistant.Core/
├── Extensions/           # Расширения
│   └── EnumExtensions.cs
├── Models/              # Модели данных
│   ├── CalendarTaskEntity.cs    # Сущность БД
│   ├── CalendarTaskModel.cs     # DTO модель
│   ├── PrioritiesLevelEnum.cs   # Уровни приоритета
│   └── TaskStatusEnum.cs        # Статусы задач
├── Protos/              # gRPC контракты
│   └── task.proto
└── Services/            # Сервисы
    ├── ITaskService.cs                 # Сервис задач
    └── ITelegramNotificationService.cs # Сервис Telegram уведомлений
```

## 📦 Основные компоненты

### Модели (Models)

- **CalendarTaskEntity** - сущность для хранения в базе данных
- **CalendarTaskModel** - модель для передачи данных между слоями
- **PrioritiesLevelEnum** - перечисление уровней приоритета задач
- **TaskStatusEnum** - перечисление статусов выполнения задач

### Сервисы (Services)

- **ITaskService** - интерфейс для операций с задачами (CRUD)
- **ITelegramNotificationService** - интерфейс для отправки уведомлений через Telegram

### Расширения (Extensions)

- **EnumExtensions** - утилитарные методы для работы с перечислениями

### gRPC контракты (Protos)

- **task.proto** - Protocol Buffers контракты для gRPC API

## 🚀 Возможности

- ✅ Управление задачами календаря
- 🎯 Приоритизация задач
- 📊 Отслеживание статусов выполнения
- 🔔 Telegram уведомления
- 🌐 gRPC API для клиентских приложений

## 🛠️ Технологии

- **C#** / .NET
- **gRPC** - для межсервисного взаимодействия
- **Protocol Buffers** - сериализация данных
- **Telegram Bot API** - система уведомлений

## Сборка NuGet пакета DeskAssistant.Core

1. Перейти в каталог библиотеки:
```bash
cd D:\Develop\DeskAssistant.Core
````

2. Собрать пакет:

```bash
dotnet pack --configuration Release
```

3. Удаляем старый источник
```bash
dotnet nuget remove source LocalDeskAssistant
```

4. Добавить локальный источник (если еще не добавлен):

```bash
dotnet nuget add source D:\Develop\DeskAssistant.Core\nuget -n LocalDeskAssistant
```

5. Проверить подключенные источники:

```bash
dotnet nuget list source
```

6. Подключить пакет к проекту DeskAssistant:

```bash
cd D:\Develop\DeskAssistant\DeskAssistant
dotnet add package DeskAssistant.Core --version 1.0.0
```

7. При изменениях версии пересобрать пакет:

```bash
dotnet pack -c Release -p:Version=1.0.1
dotnet add package DeskAssistant.Core --version 1.0.1
```

## 📁 Использование

Проект представляет собой библиотеку, которая может быть подключена к основному приложению DeskAssistant.

---